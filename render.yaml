services:
  - type: web
    name: code-vault-api
    env: node
    rootDir: server
    plan: free
    buildCommand: npm install
    startCommand: npm run dev
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      
