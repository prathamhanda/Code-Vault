# Code Vault (Frontend)

This folder contains the **React + Vite** frontend for Code Vault.

For the full project overview (features, API, deployment, etc.), see:

- `../README.md`

## Local Development

```bash
npm install
npm run dev
```

### Environment

Create `./.env`:

```env
VITE_API_BASE_URL=http://localhost:5000
```
